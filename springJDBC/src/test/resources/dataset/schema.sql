--------------------------------------------------------
--  DDL for Table EMPLOYEE
--------------------------------------------------------

CREATE TABLE "EMPLOYEE" 
   (	
    "EMPLOYEE_ID" NUMBER NOT NULL, 
	"EMPLOYEE_FIRST_NAME" VARCHAR2(512) NOT NULL, 
	"EMPLOYEE_LAST_NAME" VARCHAR2(512) NOT NULL,
	"EMPLOYEE_AGE" NUMBER NOT NULL,
	"EMPLOYEE_ADDRESS" VARCHAR2(512) NOT NULL,
	"ACTIVE_IND" NUMBER DEFAULT 1 , 
	"CREATE_DT_TM" TIMESTAMP (3) DEFAULT SYSTIMESTAMP, 
	"UPDT_DT_TM" TIMESTAMP (3) DEFAULT SYSTIMESTAMP,
	);
	
CREATE SEQUENCE EMPLOYEE_SEQ START WITH 100 INCREMENT BY 1;
   